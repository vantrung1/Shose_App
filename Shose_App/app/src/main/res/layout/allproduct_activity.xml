<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:background="@color/background"
    android:layout_height="match_parent">
    <androidx.appcompat.widget.Toolbar
        android:layout_width="match_parent"
        android:id="@+id/toolbar_allproduct"
        android:elevation="8dp"
        android:layout_marginBottom="30dp"
        android:layout_alignParentTop="true"
        android:layout_height="?attr/actionBarSize"
        android:background="@color/background">
        <FrameLayout
            android:id="@+id/layout_right_allproduct"
            android:layout_width="60dp"
            android:layout_gravity="right"
            android:layout_height="?attr/actionBarSize">
            <ImageView
                android:layout_gravity="center_horizontal"
                android:layout_marginTop="10dp"
                android:layout_width="30dp"
                android:layout_height="30dp"
                android:src="@drawable/baseline_search_24"/>
        </FrameLayout>
    </androidx.appcompat.widget.Toolbar>
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recy_allproduct"
        android:layout_width="match_parent"
        android:layout_marginBottom="10dp"
        android:layout_below="@id/toolbar_allproduct"
        android:layout_height="wrap_content"/>
    <TextView
        android:visibility="gone"
        android:layout_below="@id/recy_allproduct"
        android:layout_width="match_parent"
        android:text="See Move"

        android:layout_height="wrap_content"/>
</RelativeLayout>